import { useContext } from 'react';
import { ApiContext } from '../../utils/api_context';

export const Question3 = () => {
  const api = useContext(ApiContext);

  return <div>Render your transactions here</div>;
};
